services:
  mc-blueprint:
    env_file:
      - .env
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mc-blueprint
    restart: unless-stopped
    ports:
      - "${DOCKER_PORT:-3000}:3000"
